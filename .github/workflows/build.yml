name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  release:
    types: [published]

env:
  JAVA_VERSION: '17'
  ANDROID_SDK_VERSION: '34'
  BUILD_TOOLS_VERSION: '34.0.0'

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK
      uses: actions/setup-java@v4
      with:
        java-version: ${{ env.JAVA_VERSION }}
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      with:
        sdk-version: ${{ env.ANDROID_SDK_VERSION }}
        build-tools-version: ${{ env.BUILD_TOOLS_VERSION }}

    - name: Cache Gradle
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Give permission to gradlew
      run: chmod +x ./android/gradlew

    - name: Build Debug APK
      run: ./android/gradlew assembleDebug
      working-directory: ./android

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: ai-assistant-debug
        path: android/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 7

  build-multiple:
    name: Build Multiple APK Versions
    runs-on: ubuntu-latest
    needs: build
    
    strategy:
      matrix:
        version: [deepseek, openai, moonshot, zhipu, default]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Cache Gradle
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Configure app version
      run: |
        case "${{ matrix.version }}" in
          deepseek)
            sed -i 's/AI助手/AI助手-DeepSeek/g' android/app/src/main/res/values/strings.xml
            ;;
          openai)
            sed -i 's/AI助手/AI助手-OpenAI/g' android/app/src/main/res/values/strings.xml
            ;;
          moonshot)
            sed -i 's/AI助手/AI助手-Moonshot/g' android/app/src/main/res/values/strings.xml
            ;;
          zhipu)
            sed -i 's/AI助手/AI助手-Zhipu/g' android/app/src/main/res/values/strings.xml
            ;;
        esac

    - name: Give permission to gradlew
      run: chmod +x ./android/gradlew

    - name: Build APK
      run: ./android/gradlew assembleDebug
      working-directory: ./android

    - name: Rename APK
      run: |
        cp android/app/build/outputs/apk/debug/app-debug.apk \
           android/app/build/outputs/apk/debug/ai-assistant-${{ matrix.version }}.apk

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: ai-assistant-${{ matrix.version }}
        path: android/app/build/outputs/apk/debug/ai-assistant-${{ matrix.version }}.apk
        retention-days: 7